// @ts-check

/**
 * @namespace Alarisa_Back_Ingress_Human
 * @description Dev mock for the host ingress component.
 */

export default class Alarisa_Back_Ingress_Human {
  /**
   * @param {import('node:http').IncomingMessage} _input
   * @returns {Promise<void>}
   */
  // eslint-disable-next-line no-unused-vars
  async accept(_input) {
    // no-op: accept everything in dev mode
  }
}
