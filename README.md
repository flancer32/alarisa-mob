# @flancer32/alarisa-mob

Mobile browser and installable PWA composition package for Alarisa.

The package owns mobile-first browser presentation and PWA resources under the `Alarisa_Mob_` TeqFW namespace. Shared communication contracts belong to `@flancer32/alarisa-comm`; server authority remains in the self-hosted Alarisa runtime.

The embedded cognitive context is in `ctx/`.
