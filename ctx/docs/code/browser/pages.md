# Browser Pages

- Path: `ctx/docs/code/browser/pages.md`
- Changed: `20260716`

## Catalogue

- `authenticated contact` — package-relative `./`, published as `/mob/`, combines public authentication/enrollment states with the locked or unlocked Principal Message form.

An administrator enrollment URL uses the same page with a temporary `enrollment` query parameter; success removes it from the visible URL. No account-selection, credential-management, history, or outcome-delivery page exists.
