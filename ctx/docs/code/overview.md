# Code Overview

- Path: `ctx/docs/code/overview.md`
- Changed: `20260713`

## Code Structure

`src/` contains ECMAScript modules in the `Alarisa_Pwa_` namespace. `web/` contains resources delivered to the browser.

## Engineering Constraints

Keep browser application behavior separate from base-runtime authority and backend contracts. New source branches require a corresponding local `AGENTS.md` when their structure becomes non-obvious.
