# Filesystem Structure

- Path: `ctx/docs/filesystem.md`
- Changed: `20260713`

## Purpose

Defines the top-level repository map only.

## Root Structure

- `ctx/` — ADSM cognitive context for this package.
- `src/` — ECMAScript modules in the `Alarisa_Pwa_` namespace.
- `test/` — automated package tests.
- `web/` — browser-delivered PWA resources.

## Root Files

- `AGENTS.md` — root agent instructions.
- `package.json` — npm package metadata and scripts.
- `types.d.ts` — TypeScript declaration entry point.
- `README.md` — concise human package entry note.
